export interface CardProp {
  number: number;
  title: string;
  definition: string;
  isActive?: boolean;
}

export interface IBoardingCard {
  icon: any;
  step: string;
  heading: string;
  description: string;
}
